# TODO

- [x] Update `app/(tabs)/_layout.tsx` tab bar styling: remove `position: 'absolute'` from `tabBar` style, keep height/paddingBottom safe-area handling.
- [ ] (If needed after test) Adjust tab bar z-order/touch handling so iPhone taps register.
- [ ] Run app on iPhone (sim/device) and verify navbar/tab taps on all tabs.


